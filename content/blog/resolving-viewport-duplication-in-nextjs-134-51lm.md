---
title: "Resolving viewport duplication in Next.js 13.4"
description: "An effective solution to the issue of duplicating the viewport tag in Next.js 13 when defining the viewport directly in components."
date: "2023-07-22"
tags: ["nextjs","react","typescript"]
canonical: "https://dev.to/oler/resolving-viewport-duplication-in-nextjs-134-51lm"
devto: "https://dev.to/oler/resolving-viewport-duplication-in-nextjs-134-51lm"
readingTime: "2 min read"
coverImage: "https://media2.dev.to/dynamic/image/width=1000,height=500,fit=cover,gravity=auto,format=auto/https%3A%2F%2Fdev-to-uploads.s3.amazonaws.com%2Fuploads%2Farticles%2F0bmfj1vn9cc1f4g1hnmi.png"
featured: false
---

![nextjs](https://dev-to-uploads.s3.amazonaws.com/uploads/articles/qqk50mjp94k64p5iavaf.png)

1. [Introduction](#introduction)
2. [The Problem](#the-problem)
3. [The Solution](#the-solution)
4. [Conclusion](#conclusion)

---

## Introduction

When working with Next.js, it's common to encounter some technical issues along the way. One such problem that may arise is the duplication of the viewport tag. This article aims to address this issue specifically, providing a solution to resolve the viewport duplication problem in components in Next.js 13.

---

## The Problem

Before we present the solution, it's crucial to first understand the problem at hand. When using the viewport tag directly in the code, as shown below:

```javascript
import './globals.css'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
      </head>
      <body className={inter.className}>{children}</body>
    </html>
  )
}
```

![warning](https://dev-to-uploads.s3.amazonaws.com/uploads/articles/gytcieg8lsmi1y0pdl8y.png)

---

## The Solution

To solve this problem, we need to change the way we are defining the viewport. Instead of placing it directly in the code, we should specify it in the metadata.

The solution was discovered and documented in a [StackOverflow thread](https://stackoverflow.com/questions/76740487/override-meta-tags-in-nextjs-13-4/76740673#76740673).

```Javascript
import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
  viewport: 'width=device-width, initial-scale=1, maximum-scale=1', // <-- now here
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
```

![success](https://dev-to-uploads.s3.amazonaws.com/uploads/articles/2pgyvwij5sh6khz1ujvd.png)

---

## Conclusion

In the world of software development, we often come across technical challenges. These challenges may seem daunting at first glance, but with the power of the development community and the constant exchange of knowledge, the solution is often just a post away.
